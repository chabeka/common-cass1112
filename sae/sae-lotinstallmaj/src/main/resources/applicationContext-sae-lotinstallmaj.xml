<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
   xsi:schemaLocation="
      http://www.springframework.org/schema/beans  
      http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
      http://www.springframework.org/schema/context 
      http://www.springframework.org/schema/context/spring-context-3.0.xsd">

   <!-- Chargement des fichiers properties -->
   <bean
      class="fr.urssaf.image.sae.commons.factory.PropertiesFactory"
      factory-method="load">
      <constructor-arg ref="saeConfigResource" />
   </bean>
   
   <!-- Création du bean de connexion à DFCE -->
   <bean id="dfceConfig" class="fr.urssaf.image.sae.lotinstallmaj.modele.DfceConfig">
      <property name="urlToolkit" value="${db.urlToolkit}" />
      <property name="login" value="${db.login}" />
      <property name="password" value="${db.password}" />
      <property name="basename" value="${db.baseName}" />
      <property name="timeOut" value="${db.timeout}" />
   </bean>
   
   <!-- Création du bean de connexion à Cassandra -->
   <bean id="cassandraConfig" class="fr.urssaf.image.sae.lotinstallmaj.modele.CassandraConfig">
      <property name="hosts" value="${cassandra.hosts}" />
      <property name="login" value="${cassandra.username}" />
      <property name="password" value="${cassandra.password}" />
      <property name="keyspaceName" value="${cassandra.keyspace}" />
   </bean>
   
   <!-- Gestion de message à partir d'un fichier -->
   <bean id="messageSource_sae_lotservice"
      class="org.springframework.context.support.ResourceBundleMessageSource">
      <property name="basename" value="maj_lotservice_messages" />
   </bean>
   
   <!-- Le componant-scan -->
   <context:component-scan base-package="fr.urssaf.image.sae.lotinstallmaj" />
   
</beans>