<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

   <modelVersion>4.0.0</modelVersion>

   <groupId>fr.urssaf.image.sae</groupId>
   <artifactId>sae</artifactId>
   <version>12.9.10.4-SNAPSHOT</version>

   <packaging>pom</packaging>

   <name>sae</name>


   <properties>

      <!-- Encodage de caractères -->
      <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

      <!-- Version des plug-ins Maven -->
      <maven-compiler-plugin.version>2.3.2</maven-compiler-plugin.version>
      <maven-source-plugin.version>2.1.2</maven-source-plugin.version>
      <maven-resources-plugin.version>2.5</maven-resources-plugin.version>
      <maven-javadoc-plugin.version>2.8.1</maven-javadoc-plugin.version>
      <sonar-maven-plugin.version>1.0-beta-2</sonar-maven-plugin.version>
      <maven-release-plugin.version>2.2.1</maven-release-plugin.version>
      <aspectj-maven-plugin.version>1.3</aspectj-maven-plugin.version>
      <maven-pmd-plugin.version>2.5</maven-pmd-plugin.version>
      <maven-shade-plugin.version>1.4</maven-shade-plugin.version>
      <maven-antrun-plugin.version>1.6</maven-antrun-plugin.version>
      <maven-assembly-plugin.version>2.2.2</maven-assembly-plugin.version>
      <maven-surefire-plugin.version>2.12</maven-surefire-plugin.version>
      <maven-replacer-plugin.version>1.3.6</maven-replacer-plugin.version>


      <!--
         =========================================================
         Version des dépendances des librairies réutilisables Image
         =========================================================
      -->


      <!--
         ========================================== commons-cassandra
         ==========================================
      -->
      <commons.cassandra>0.0.5</commons.cassandra>

      <!--
         Modèle de dépendance : <dependency>
         <groupId>fr.urssaf.image.commons</groupId>
         <artifactId>commons-cassandra</artifactId>
         <version>${commons.cassandra}</version> </dependency>
      -->


      <!--
         ==========================================
         commons-cassandra-spring-batch
         ==========================================
      -->
      <commons.cassandra.spring.batch>0.0.5</commons.cassandra.spring.batch>

      <!--
         Modèle de dépendance : <dependency>
         <groupId>fr.urssaf.image.commons</groupId>
         <artifactId>commons-cassandra-spring-batch</artifactId>
         <version>${commons.cassandra.spring.batch}</version>
         </dependency>
      -->


      <!--
         ========================================== commons-dfce
         ==========================================
      -->
      <commons.dfce>1.1.0-CRTL-2</commons.dfce>

      <!--
         Modèle de dépendance : <dependency>
         <groupId>fr.urssaf.image.commons</groupId>
         <artifactId>commons-dfce</artifactId>
         <version>${commons.dfce}</version> </dependency>
      -->




      <!--
         =========================================================
         Version des framework/dépendances
         =========================================================
      -->


      <!--
         ========================================== JUnit

         http://junit.org/ ==========================================
      -->
      <junit.version>4.8.2</junit.version>

      <!--
         Modèle de dépendance : <dependency> <groupId>junit</groupId>
         <artifactId>junit</artifactId>
         <version>${junit.version}</version> <scope>test</scope>
         </dependency>
      -->


      <!--
         ========================================== SLF4J (Simple
         Logging Facade for Java) http://www.slf4j.org/
         ==========================================
      -->
      <org.slf4j.version>1.6.1</org.slf4j.version>


      <!--
         ========================================== Ponts entre SLF4J et
         d'autres frameworks de log http://www.slf4j.org/legacy.html
         ==========================================
      -->

      <!-- JCL (Jakarta Commons Logging) vers SLF4J -->
      <jcl-over-slf4j.version>1.6.1</jcl-over-slf4j.version>

      <!-- Log4J vers SLF4J -->
      <log4j-over-slf4j.version>1.6.1</log4j-over-slf4j.version>


      <!--
         Modèle de dépendance : <dependency>
         <groupId>org.slf4j</groupId>
         <artifactId>jcl-over-slf4j</artifactId>
         <version>${jcl-over-slf4j.version}</version>
         <scope>runtime</scope> </dependency> <dependency>
         <groupId>org.slf4j</groupId>
         <artifactId>log4j-over-slf4j</artifactId>
         <version>${log4j-over-slf4j.version}</version>
         <scope>runtime</scope> </dependency>
      -->


      <!--
         ========================================== Logback

         http://logback.qos.ch/
         ==========================================
      -->
      <logback.version>0.9.30</logback.version>


      <!--
         ========================================== Spring

         http://www.springsource.org/
         ==========================================
      -->
      <spring.framework.version>3.0.5.RELEASE</spring.framework.version>
      <spring.security.version>3.0.5.RELEASE</spring.security.version>
      <spring.batch.version>2.1.8.RELEASE</spring.batch.version>

      <!--
         Exemples de dépendance : Pour Spring Core : <dependency>
         <groupId>org.springframework</groupId>
         <artifactId>spring-context</artifactId>
         <version>${spring.framework.version}</version> </dependency>

         <dependency> <groupId>org.springframework</groupId>
         <artifactId>spring-test</artifactId>
         <version>${spring.framework.version}</version>
         <scope>test</scope> </dependency> Pour Spring Security :

         <dependency> <groupId>org.springframework.security</groupId>
         <artifactId>spring-security-config</artifactId>
         <version>${spring.security.version}</version> </dependency>


         Pour Spring Batch : <dependency>
         <groupId>org.springframework.batch</groupId>
         <artifactId>spring-batch-core</artifactId>
         <version>${spring.batch.version}</version> </dependency>

         <dependency> <groupId>org.springframework.batch</groupId>
         <artifactId>spring-batch-test</artifactId>
         <version>${spring.batch.version}</version> <scope>test</scope>
         </dependency>
      -->


      <!--
         ========================================== Apache Commons

         http://commons.apache.org/
         ==========================================
      -->

      <apache.commons-configuration.version>1.6</apache.commons-configuration.version>
      <apache.commons-collections.version>3.2.1</apache.commons-collections.version>
      <apache.commons-io.version>2.0.1</apache.commons-io.version>
      <apache.commons-lang.version>2.6</apache.commons-lang.version>
      <apache.commons-codec.version>1.4</apache.commons-codec.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>commons-configuration</groupId>
         <artifactId>commons-configuration</artifactId>
         <version>${apache.commons-configuration.version}</version>
         <scope>test</scope> </dependency> <dependency>
         <groupId>commons-collections</groupId>
         <artifactId>commons-collections</artifactId>
         <version>${apache.commons-collections.version}</version>
         <scope>test</scope> </dependency> <dependency>
         <groupId>commons-io</groupId>
         <artifactId>commons-io</artifactId>
         <version>${apache.commons-io.version}</version> </dependency>

         <dependency> <groupId>commons-lang</groupId>
         <artifactId>commons-lang</artifactId>
         <version>${apache.commons-lang.version}</version> </dependency>

         <dependency> <groupId>commons-codec</groupId>
         <artifactId>commons-codec</artifactId>
         <version>${apache.commons-codec.version}</version>
         </dependency>
      -->


      <!--
         ========================================== JDOM

         http://www.jdom.org/ ==========================================
      -->
      <org.jdom.version>1.1</org.jdom.version>

      <!--
         Modèle de dépendance : <dependency> <groupId>org.jdom</groupId>
         <artifactId>jdom</artifactId>
         <version>${org.jdom.version}</version> </dependency>
      -->


      <!--
         ========================================== Joda Time (Java date
         and time API) http://joda-time.sourceforge.net/
         ==========================================
      -->
      <joda-time.version>2.0</joda-time.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>joda-time</groupId> <artifactId>joda-time</artifactId>
         <version>${joda-time.version}</version> <scope>runtime</scope>
         </dependency>
      -->


      <!--
         ========================================== Java Servlet API

         Spécifications de la version 2.3 (JSR 53) :
         http://www.jcp.org/en/jsr/detail?id=53 Spécifications des
         versions 2.4 et 2.5 (JSR 154) :
         http://www.jcp.org/en/jsr/detail?id=154 Spécifications de la
         version 3.0 (JSR 315) : http://www.jcp.org/en/jsr/detail?id=315
         ==========================================
      -->
      <javax.servlet.version>2.5</javax.servlet.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>javax.servlet</groupId>
         <artifactId>servlet-api</artifactId>
         <version>${javax.servlet.version}</version>
         <scope>provided</scope> </dependency>
      -->


      <!--
         ========================================== AspectJ

         http://www.eclipse.org/aspectj/
         ==========================================
      -->
      <aspectj.version>1.6.10</aspectj.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>org.aspectj</groupId>
         <artifactId>aspectjrt</artifactId>
         <version>${aspectj.version}</version> </dependency>
      -->


      <!--
         ========================================== Axis 2

         http://ws.apache.org/axis/
         ==========================================
      -->
      <axis2.version>1.6.1</axis2.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>org.apache.axis2</groupId>
         <artifactId>axis2</artifactId>
         <version>${axis2.version}</version> </dependency> <dependency>
         <groupId>org.apache.axis2</groupId>
         <artifactId>axis2-adb</artifactId>
         <version>${axis2.version}</version> </dependency> <dependency>
         <groupId>org.apache.axis2</groupId>
         <artifactId>axis2-transport-local</artifactId>
         <version>${axis2.version}</version> </dependency> <dependency>
         <groupId>org.apache.axis2</groupId>
         <artifactId>axis2-transport-http</artifactId>
         <version>${axis2.version}</version> </dependency>
      -->


      <!--
         ========================================== XStream

         http://xstream.codehaus.org/
         ==========================================
      -->
      <xstream.version>1.3.1</xstream.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>com.thoughtworks.xstream</groupId>
         <artifactId>xstream</artifactId>
         <version>${xstream.version}</version> <scope>test</scope>
         </dependency>
      -->


      <!--
         ========================================== EasyMock

         http://www.easymock.org/
         ==========================================
      -->
      <easymock.version>3.0</easymock.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>org.easymock</groupId>
         <artifactId>easymock</artifactId>
         <version>${easymock.version}</version> <scope>test</scope>
         </dependency>
      -->


      <!--
         ========================================== JUnitPerf

         http://www.clarkware.com/software/JUnitPerf.html
         ==========================================
      -->
      <junitperf.version>1.9.1</junitperf.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>junitperf</groupId> <artifactId>junitperf</artifactId>
         <version>${junitperf.version}</version> <scope>test</scope>
         </dependency>
      -->


      <!--
         ========================================== Google Guava

         http://code.google.com/p/guava-libraries/
         ==========================================
      -->

      <guava.version>12.0</guava.version>

      <!--
         Modèles de dépendance : <dependency>
         <groupId>com.google.guava</groupId>
         <artifactId>guava</artifactId>
         <version>${guava.version}</version> </dependency>
      -->


   </properties>


   <!-- Les modules -->
   <modules>

      <!-- Les composants techniques -->
      <module>sae-bo</module>
      <module>sae-igc</module>
      <module>sae-saml</module>
      <module>sae-vi</module>
      <module>sae-storage</module>
      <module>sae-storage-dfce</module>
      <module>sae-ecde</module>
      <module>sae-metadata</module>
      <module>sae-mapping</module>
      <module>sae-pile-travaux</module>
      <module>sae-services</module>
      <module>sae-droit</module>

      <!-- Les applications finales -->
      <module>sae-igcmaj</module>
      <module>sae-services-executable</module>
      <module>sae-webservices</module>
      <module>sae-dfce-admin-exploit</module>
      <module>sae-ordonnanceur</module>
      <module>sae-lotinstallmaj</module>
      
       <!-- Le module du livrable -->
       <module>sae-livrable</module>

   </modules>

   <!-- Configuration SVN pour le plug-in maven-release -->
   <scm>
      <url>
        scm:svn:svn://cer69-svn.cer69.recouv/image/java/trunk/sae/
      </url>
      <developerConnection>
         scm:svn:svn://cer69-svn.cer69.recouv/image/java/trunk/sae/
      </developerConnection>
      <connection>
         scm:svn:svn://cer69-svn.cer69.recouv/image/java/trunk/sae/
      </connection>
   </scm>


   <build>
      <plugins>

         <!--
            On spécifie la version du JDK et l'encodage de caractères
            des sources
         -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>${maven-compiler-plugin.version}</version>
            <configuration>
               <source>1.6</source>
               <target>1.6</target>
               <encoding>${project.build.sourceEncoding}</encoding>
            </configuration>
         </plugin>


         <!--
            Dans le cas d'un packaging JAR, on demande que le JAR des
            sources soit également généré
         -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>${maven-source-plugin.version}</version>
            <executions>
               <execution>
                  <id>bind-sources</id>
                  <goals>
                     <goal>jar</goal>
                  </goals>
               </execution>
            </executions>
         </plugin>


         <!--
            On spécifie l'encodage de caractères des ressources
         -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-resources-plugin</artifactId>
            <version>${maven-resources-plugin.version}</version>
            <configuration>
               <encoding>${project.build.sourceEncoding}</encoding>
            </configuration>
         </plugin>


         <!--
         On demande la génération de la Javadoc
         -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>${maven-javadoc-plugin.version}</version>
            <executions>
               <execution>
                  <id>attach-javadocs</id>
                  <goals>
                     <goal>jar</goal>
                  </goals>
               </execution>
            </executions>
            <configuration>
               <quiet>true</quiet>
            </configuration>
         </plugin>


         <!--
            On spécifie la version du plugin Maven pour Sonar
         -->
         <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>sonar-maven-plugin</artifactId>
            <version>${sonar-maven-plugin.version}</version>
         </plugin>


         <!--
         Pour la gestion des release
         -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-release-plugin</artifactId>
            <version>${maven-release-plugin.version}</version>
            <configuration>
               <preparationGoals>clean javadoc:jar install</preparationGoals>
               <autoVersionSubmodules>true</autoVersionSubmodules>
               <useReleaseProfile>false</useReleaseProfile>
            </configuration>
         </plugin>

      </plugins>


      <pluginManagement>

         <plugins>

            <!-- COMPILATION ASPECT -->
            <plugin>
               <groupId>org.codehaus.mojo</groupId>
               <artifactId>aspectj-maven-plugin</artifactId>
               <version>${aspectj-maven-plugin.version}</version>
               <configuration>
                  <source>1.6</source>
                  <Xlint>ignore</Xlint>
               </configuration>
               <executions>
                  <execution>
                     <goals>
                        <goal>compile</goal>
                        <goal>test-compile</goal>
                     </goals>
                  </execution>
               </executions>
            </plugin>

         </plugins>

      </pluginManagement>

   </build>

   <dependencies>

      <!-- JUnit -->
      <dependency>
         <groupId>junit</groupId>
         <artifactId>junit</artifactId>
         <version>${junit.version}</version>
         <scope>test</scope>
      </dependency>

      <!-- SLF4J et Logback -->
      <dependency>
         <groupId>org.slf4j</groupId>
         <artifactId>slf4j-api</artifactId>
         <version>${org.slf4j.version}</version>
      </dependency>
      <dependency>
         <groupId>ch.qos.logback</groupId>
         <artifactId>logback-classic</artifactId>
         <version>${logback.version}</version>
      </dependency>
      <dependency>
         <groupId>ch.qos.logback</groupId>
         <artifactId>logback-core</artifactId>
         <version>${logback.version}</version>
      </dependency>

   </dependencies>


   <reporting>
      <plugins>

         <!--
            Paramétrage de la génération des rapports PMD
         -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-pmd-plugin</artifactId>
            <version>${maven-pmd-plugin.version}</version>
            <configuration>
               <linkXref>false</linkXref>
               <sourceEncoding>${project.build.sourceEncoding}</sourceEncoding>
               <minimumTokens>100</minimumTokens>
               <targetJdk>1.6</targetJdk>
               <rulesets>
                  <ruleset>http://cer69-hudson2.cer69.recouv:80/pmd_rules/pmd_sae-parent.xml</ruleset>
               </rulesets>
            </configuration>
         </plugin>

      </plugins>
   </reporting>


   <!--
      On indique l'URL des repository Nexus, pour les versions Snapshots
      et Releases des artéfacts
   -->
   <distributionManagement>
      <repository>
         <id>nexus</id>
         <name>Releases</name>
         <url>http://cer69-nexus.cer69.recouv:8081/nexus/content/repositories/releases</url>
      </repository>
      <snapshotRepository>
         <id>nexus</id>
         <name>Snapshots</name>
         <url>http://cer69-nexus.cer69.recouv:8081/nexus/content/repositories/snapshots</url>
         <uniqueVersion>false</uniqueVersion>
      </snapshotRepository>
   </distributionManagement>

</project>