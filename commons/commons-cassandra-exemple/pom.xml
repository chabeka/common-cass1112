<project
   xmlns="http://maven.apache.org/POM/4.0.0"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
   
   <modelVersion>4.0.0</modelVersion>
   
   <groupId>fr.urssaf.image.commons</groupId>
   <artifactId>commons-cassandra-exemple</artifactId>
   <version>0.0.1-SNAPSHOT</version>
   
   <name>commons-cassandra-exemple</name>
   
   <parent>
      <groupId>fr.urssaf.image.commons</groupId>
      <artifactId>commons-parent</artifactId>
      <version>0.0.1-SNAPSHOT</version>
   </parent>
   
   <properties>
      <logback.version>0.9.30</logback.version>
      <org.slf4j.version>1.6.1</org.slf4j.version>
      <commons.cassandra.version>0.0.4</commons.cassandra.version>
   </properties>


   <build>
      <plugins>

         <!-- Skip test -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>2.5</version>
            <configuration>
               <skipTests>false</skipTests>
            </configuration>
         </plugin>
         
      </plugins>
      
   </build>
         
   
   <dependencies>
   
      <!-- Log back -->
      <dependency>
         <groupId>ch.qos.logback</groupId>
         <artifactId>logback-classic</artifactId>
         <version>${logback.version}</version>
      </dependency>
      <dependency>
         <groupId>ch.qos.logback</groupId>
         <artifactId>logback-core</artifactId>
         <version>${logback.version}</version>
      </dependency>
      <dependency>
         <groupId>org.slf4j</groupId>
         <artifactId>slf4j-api</artifactId>
         <version>${org.slf4j.version}</version>
      </dependency>
      

      <!-- commons-cassandra -->
      <dependency>
         <groupId>fr.urssaf.image.commons</groupId>
         <artifactId>commons-cassandra</artifactId>
         <version>${commons.cassandra.version}</version>
       </dependency>
      
   </dependencies>
   
</project>
