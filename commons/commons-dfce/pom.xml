<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
   <modelVersion>4.0.0</modelVersion>
   <groupId>fr.urssaf.image.commons</groupId>
   <artifactId>commons-dfce</artifactId>
   <name>commons-dfce</name>
   <version>1.0.1-CRTL-1</version>
   <description>module pour l'intégration de DFCE</description>

   <properties>
      <!-- Encodage de caractères -->
      <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

      <!-- numéro de version des dépendances de DFCE -->
      <dfce.serialization>1.0.1</dfce.serialization>
      <dfce.toolkit.client>1.0.1</dfce.toolkit.client>
      <dfce.toolkit.pojo>1.0.1</dfce.toolkit.pojo>
      <dfce.pojo>1.0.1</dfce.pojo>
      <dfce.toolkit.service>1.0.1</dfce.toolkit.service>
      <dfce.commons>1.0.1</dfce.commons>
   </properties>

   <dependencies>

      <!-- DFCE -->

      <dependency>
         <groupId>net.docubase</groupId>
         <artifactId>dfce-serialization</artifactId>
         <version>${dfce.serialization}</version>
      </dependency>

      <dependency>
         <groupId>com.docubase</groupId>
         <artifactId>dfce-pojo</artifactId>
         <version>${dfce.pojo}</version>
      </dependency>

      <dependency>
         <groupId>net.docubase</groupId>
         <artifactId>dfce-toolkit-client</artifactId>
         <version>${dfce.toolkit.client}</version>
      </dependency>

      <dependency>
         <groupId>net.docubase</groupId>
         <artifactId>dfce-toolkit-pojo</artifactId>
         <version>${dfce.toolkit.pojo}</version>
      </dependency>

      <dependency>
         <groupId>net.docubase</groupId>
         <artifactId>dfce-toolkit-service</artifactId>
         <version>${dfce.toolkit.service}</version>
      </dependency>

      <dependency>
         <groupId>net.docubase</groupId>
         <artifactId>dfce-commons</artifactId>
         <version>${dfce.commons}</version>
      </dependency>

      <!-- COMMONS APACHE -->

      <dependency>
         <groupId>commons-io</groupId>
         <artifactId>commons-io</artifactId>
         <version>1.4</version>
      </dependency>

      <dependency>
         <groupId>commons-lang</groupId>
         <artifactId>commons-lang</artifactId>
         <version>2.5</version>
      </dependency>

      <!-- LOG4J -->

      <dependency>
         <groupId>log4j</groupId>
         <artifactId>log4j</artifactId>
         <version>1.2.16</version>
      </dependency>

      <!-- LUCENE -->

      <dependency>
         <groupId>org.apache.lucene</groupId>
         <artifactId>lucene-core</artifactId>
         <version>2.9.1</version>
      </dependency>

      <dependency>
         <groupId>org.apache.lucene</groupId>
         <artifactId>lucene-queryparser</artifactId>
         <version>2.9.1</version>
      </dependency>

      <!-- HECTOR -->

      <dependency>
         <groupId>me.prettyprint</groupId>
         <artifactId>hector-core</artifactId>
         <version>1.0-3</version>
         <exclusions>
            <exclusion>
               <groupId>com.google.guava</groupId>
               <artifactId>guava</artifactId>
            </exclusion>
            <exclusion>
               <groupId>junit</groupId>
               <artifactId>junit</artifactId>
            </exclusion>
         </exclusions>
      </dependency>

      <!-- GUAVA -->

      <dependency>
         <groupId>com.google.guava</groupId>
         <artifactId>guava</artifactId>
         <version>10.0.1</version>
      </dependency>

      <!-- JUNIT -->

      <dependency>
         <groupId>junit</groupId>
         <artifactId>junit</artifactId>
         <version>4.8.2</version>
         <scope>test</scope>
      </dependency>

      <!-- SPRING pour les tests -->

      <dependency>
         <groupId>org.springframework</groupId>
         <artifactId>spring-tx</artifactId>
         <version>3.0.5.RELEASE</version>
         <scope>test</scope>
      </dependency>

      <dependency>
         <groupId>org.springframework</groupId>
         <artifactId>spring-beans</artifactId>
         <version>3.0.5.RELEASE</version>
         <scope>test</scope>
      </dependency>

      <dependency>
         <groupId>org.springframework</groupId>
         <artifactId>spring-test</artifactId>
         <version>3.0.5.RELEASE</version>
         <scope>test</scope>
      </dependency>

   </dependencies>

   <!-- Configuration SVN pour le plug-in maven-release -->
   <scm>
      <url>
        scm:svn:svn://cer69-svn.cer69.recouv/image/java/tags/commons-dfce-1.0.1-CRTL-1
      </url>
      <developerConnection>
         scm:svn:svn://cer69-svn.cer69.recouv/image/java/tags/commons-dfce-1.0.1-CRTL-1
      </developerConnection>
      <connection>
         scm:svn:svn://cer69-svn.cer69.recouv/image/java/tags/commons-dfce-1.0.1-CRTL-1
      </connection>
   </scm>

   <build>
      <plugins>

         <!--
            On spécifie la version du JDK et l'encodage de caractères
            des sources
         -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <configuration>
               <source>1.6</source>
               <target>1.6</target>
               <encoding>${project.build.sourceEncoding}</encoding>
            </configuration>
         </plugin>


         <!--
            Dans le cas d'un packaging JAR, on demande que le JAR des
            sources soit également généré
         -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>2.1.2</version>
            <executions>
               <execution>
                  <id>bind-sources</id>
                  <goals>
                     <goal>jar</goal>
                  </goals>
               </execution>
            </executions>
         </plugin>


         <!-- On spécifie l'encodage de caractères des ressources-->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-resources-plugin</artifactId>
            <configuration>
               <encoding>${project.build.sourceEncoding}</encoding>
            </configuration>
         </plugin>

         <!-- On demande la génération de la Javadoc -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>2.7</version>
            <executions>
               <execution>
                  <id>attach-javadocs</id>
                  <goals>
                     <goal>jar</goal>
                  </goals>
               </execution>
            </executions>
         </plugin>

         <!-- Pour la gestion des release -->
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-release-plugin</artifactId>
            <version>2.2.1</version>
            <configuration>
               <preparationGoals>clean javadoc:jar install</preparationGoals>
               <autoVersionSubmodules>true</autoVersionSubmodules>
               <useReleaseProfile>false</useReleaseProfile>
            </configuration>
         </plugin>

      </plugins>

   </build>

   <!--
      On indique l'URL des repository Nexus, pour les versions Snapshots
      et Releases des artéfacts
   -->
   <distributionManagement>
      <repository>
         <id>nexus</id>
         <name>Releases</name>
         <url>http://cer69-nexus.cer69.recouv:8081/nexus/content/repositories/releases</url>
      </repository>
      <snapshotRepository>
         <id>nexus</id>
         <name>Snapshots</name>
         <url>http://cer69-nexus.cer69.recouv:8081/nexus/content/repositories/snapshots</url>
         <uniqueVersion>false</uniqueVersion>
      </snapshotRepository>
   </distributionManagement>


</project>